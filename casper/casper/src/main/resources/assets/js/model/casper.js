/**
 *    This is where business logic lives. Validation, service calls, etc. all go here
 *    Take your time and design a really good model
 *    NOTE: All model definitions should go in the global 'EG' object
 */

(function () {
    "use strict";

    // Model
    EG.Casper = Backbone.Model.extend({
        // See backbonejs.org and search Confluence for 'Backbone Patterns' to learn more
    });

    // Collection
    EG.CasperCollection = Backbone.Collection.extend({
        //model: EG.Casper,
        //url: "/casper"
    });

})();